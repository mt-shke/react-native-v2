import { View, Text } from "react-native";

const CircuitScreen = (props) => {
    return (
        <View>
            <Text>CircuitScreen</Text>
        </View>
    );
};

export default CircuitScreen;
